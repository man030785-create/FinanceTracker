# Copy to .env and set values. SECRET_KEY is required for JWT signing.
SECRET_KEY=your-secret-key-change-in-production

# Database: SQLite (default) or Postgres (for production / Render)
# SQLite (default if DATABASE_URL is not set):
# DATABASE_URL=sqlite:///./financetracker.db
# Postgres (local example; Render provides this automatically):
# DATABASE_URL=postgresql://user:password@localhost:5432/financetracker
